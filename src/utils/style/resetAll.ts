import { css } from '@emotion/core';

const resetAll = css`
  position: static;
  float: none;
  width: auto;
  height: auto;
  clear: both;
  margin: 0;
  border: 0;
  padding: 0;
`;

export default resetAll;
